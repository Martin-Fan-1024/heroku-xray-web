{
    "inbounds": [
        {
            "listen": "/etc/caddy/vless", 
            "protocol": "vless", 
            "settings": {
                "clients": [
                    {
                        "id": "$UUID"
                    }
                ], 
                "decryption": "none"
            }, 
            "streamSettings": {
                "network": "ws", 
                "wsSettings": {
                    "path": "/api"
                }
            }
        },
        {
      "port": 2002,
      "listen": "127.0.0.1",
      "protocol": "vless",
      "settings": {
        "clients": [
          {
            "id": "$UUID" // 填写你的 UUID
          }
        ],
        "decryption": "none"
      },
      "streamSettings": {
        "network": "grpc",
        "grpcSettings": {
          "serviceName": "api" // 填写你的 ServiceName
        }
      }
    }
    ], 
    "outbounds": [
        {
            "protocol": "freedom"
        }
    ]
}
